image: node:6.9.4

cache:
  paths:
  - node_modules/

stages:
  - build
  - test

build_job:
  stage: build
  script:
   - npm install
   - npm build

test_job:
  stage: test
  script:
   - npm install
   - npm test
